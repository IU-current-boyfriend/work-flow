import JobBaseApproval from "./JobBaseApproval";

class JobAutomaticConsent extends JobBaseApproval {
  // 构造函数
  constructor(approvalType) {
    super(approvalType);
  }
}

export default JobAutomaticConsent;
